export interface IUSER {
  name: string;
  email: string;
  active: boolean;
  role: string;
  id: number;
}

export interface IFormUser {
  name: string;
  email: string;
  active: boolean;
  role: string;
}
